# 🗺️ 드래곤 빌리지 3 카드교환소 마스터 청사진 (MASTER_BLUEPRINT)

본 문서는 프로젝트의 전�## 🏗️ 시스템 아키텍처 (v2.5)

```mermaid
graph TD
    WithHive[위하이브 커뮤니티 글 312801] -->|1. Ulixee Hero 동적 스크래핑| Scraper[scripts/scraper.js]
    Scraper -->|2. scraped_images.json 저장| DataUpdate[scripts/updateStickersData.js]
    DataUpdate -->|3. 180개 CDN 주소 매핑| StickersData[src/stickersData.js]
    
    User([사용자]) -->|4. 역할 분기| Gateway{구해요/팝니다 선택}
    Gateway -->|구매자 buyer| PageListB[800px 단독 1열: 20개 카테고리 1번 드래곤 대표 썸네일]
    Gateway -->|판매자 seller| PageListS[800px 단독 1열: 20개 카테고리 1번 드래곤 대표 썸네일]
    
    StickersData -->|5. CDN 180장 이미지 공급| PageListB
    StickersData -->|5. CDN 180장 이미지 공급| PageListS
    
    PageListB -->|클릭: 800px 거대 1열 중앙 레이아웃| Grid3x3_B[3x3 세로형 카드 그리드: 드래곤 등급 별★ 표시 & 획득 컬러 피드백 & 슬림한 이름 라벨]
    PageListS -->|클릭: 800px 거대 1열 중앙 레이아웃| Grid3x3_S[3x3 세로형 카드 그리드: 드래곤 등급 별★ 표시 & 획득 컬러 피드백 & 슬림한 이름 라벨]
    
    Grid3x3_B -->|등록: 하단 정렬된 글래스모피즘 폼| DB[(데이터베이스: Supabase / LocalStorage)]
    Grid3x3_S -->|등록: 하단 정렬된 글래스모피즘 폼| DB
    
    DB -->|실시간 상호 교차 매칭| MatchEngine[매칭 엔진]
    MatchEngine -->|결과 피드 출력| Feed[매칭 성사 뱃지 & 시나리오 렌더링]
```

---

## 📂 파일 구조 설명

이 프로젝트의 핵심 파일들은 다음과 같이 배치되어 있습니다:

- **`index.html`**
  - 사이트의 뼈대가 되는 파일입니다. 검색엔진 노출을 위한 정보(SEO)와 한국어 설정이 적용되어 있습니다.
- **`src/main.jsx`**
  - React 앱의 진입점으로, `App.jsx`와 `index.css`를 연결해 브라우저에 화면을 띄워주는 역할을 합니다.
- **`src/App.jsx`**
  - **애플리케이션의 핵심**입니다. 첫 진입 게이트웨이 화면, 20개 정규 카테고리 뷰, 3x3 스티커 선택창, 그리고 구매자-판매자간 교차 매칭 알고리즘이 들어 있습니다.
- **`src/index.css`**
  - 앱 전체의 스타일 시트입니다. 다크 네온 보라/핑크 테마에서 인게임 감성의 묵직한 다크 그레이와 네온 연두/노랑 테마 스타일링으로 리모델링되었습니다.
- **`src/stickersData.js`**
  - 위하이브 동적 크래핑을 통해 빌드된 **180장의 정규 스티커 초고화질 CDN 주소 목록**과 카테고리별 1~9번 매핑 알고리즘이 포함되어 있습니다.
- **`src/supabaseClient.js`**
  - 데이터 저장소와 통신을 담당합니다. 데이터베이스 연결이 되지 않았을 때도 사용자가 사이트를 시험 운전해볼 수 있도록 "가짜 브라우저 저장소(LocalStorage)" 기능을 백업으로 품고 있습니다.
- **`scripts/` (데이터 수집 도구 모음)**
  - `scraper.js`: Ulixee Hero 동적 안티봇 우회 브라우저를 띄워 위하이브 공식 글에서 본문 180장의 이미지 src를 순서대로 긁어옵니다.
  - `updateStickersData.js`: 긁어온 180개의 CDN 주소를 리액트 스티커 데이터 파일(`src/stickersData.js`) 구조에 맞게 자동 트랜스폼 및 덮어쓰기하는 데이터 빌드 유틸리티입니다.

---

## ⚡ 교환 매칭의 원리 (v2)

1. **구매자 A**가 `야생몬스터 5번 슬롯` 카드를 구한다고 올립니다.
2. **판매자 B**가 `야생몬스터 5번 슬롯` 카드를 판다고 올립니다.
3. 매칭 엔진이 두 유저의 **역할(buyer vs seller)**과 **카드 ID(`1-5`)**를 실시간으로 비교하여, 서로 정확히 교차되는 건에 대해 **`⚡ 교환 가능!`** 뱃지를 달아줍니다.

---

## 🗓️ 업데이트 기록 (2026-06-04)
- **최초 프로젝트 생성**: Vite React 기반의 프로젝트 뼈대 구축.
- **v2 페이지 & 3x3 그리드 개편**: 구매자/판매자 분기 게이트웨이 및 3x3 슬롯 토글 선택기 탑재.
- **v2.1 카테고리 정제**: 중복 파일 제외한 20개 카테고리 정제 완료 및 누락 카테고리 방어 코딩 적용.
- **v2.2 위하이브 180장 CDN 실시간 맵핑**:
  - Ulixee Hero 크롤러로 위하이브 스티커 가이드 글에서 180장의 실물 이미지 CDN 추출 성공.
  - 3x3 그리드 각각의 슬롯 배경에 **드래곤 실물 카드 렌더러** 장착.
  - 누락된 3개 카테고리(유령 마을, 빛 아래, 하늘나라)를 포함한 20개 전 영역에 실물 카드 3x3 비주얼 완전 매핑.
- **v2.3 카드명 노출 및 카테고리 UI 격리**:
  - 3x3 각 상세 카드 슬롯 하단에 실제 한글 드래곤 명칭을 연동 및 출력.
  - 카테고리 뷰 카드를 대표이미지(contain 비율) + 이름 구조로 나누어 겹침 없는 배열 렌더링 완료.
- **v2.4 인게임 테마 벤치마킹 및 레이아웃 확장**:
  - 동영상 피드백을 기반으로 **다크 그레이 & 네온 연두/황금 별** 감성의 인게임 디자인 테마를 고스란히 이식.
  - 3x3 도감 진입 시 가로 분할을 풀고 **800px 가로폭의 거대 1열 중앙 집중형 레이아웃** 구현.
  - 개별 카드 비율을 게임 카드의 **세로형 비율(aspectRatio: 0.78)**로 튜닝하고 미선택 시 **흑백/어두운 실루엣 연출 및 회색 별 5성**, 선택 시 **컬러 활성화 및 황금 별 5성** 기믹 도입.
  - 뒤로가기("카테고리 목록") 버튼을 타이틀 최좌측에 고정시켜 정렬 안정성 확보.
  - 상단 로고("DRAGON VILLAGE 3")를 클릭하면 역할 선택 홈으로 복귀하는 UX 동작 추가.
- **v2.5 레이아웃 1열 통합 및 대표 이미지 최적화 [LATEST]**:
  - 메인 카테고리 화면을 좌우 분할 구조에서 **800px 가로폭 단독 1열 수직 레이아웃**으로 전면 리빌딩하고, 장바구니 영역을 하단에 통합 정렬.
  - 카테고리 카드의 대표 이미지를 캡쳐본 대신 **해당 카테고리의 1번 드래곤 단독 일러스트 CDN**으로 100% 전면 교체.
  - 카테고리 목록 카드 밑에 표시되던 지저분한 **`0/9` 등의 개수 획득 배지를 완전히 삭제**.
  - 상세 도감 그리드 속 카드의 **하단 이름칸 세로 비율을 슬림하게 축소**하고, 드래곤 이미지 높이를 `70%`로 확장하여 캐릭터 실물을 극대화.ckersData.js`: 긁어온 180개의 CDN 주소를 리액트 스티커 데이터 파일(`src/stickersData.js`) 구조에 맞게 자동 트랜스폼 및 덮어쓰기하는 데이터 빌드 유틸리티입니다.

---

## ⚡ 교환 매칭의 원리 (v2)

1. **구매자 A**가 `야생몬스터 5번 슬롯` 카드를 구한다고 올립니다.
2. **판매자 B**가 `야생몬스터 5번 슬롯` 카드를 판다고 올립니다.
3. 매칭 엔진이 두 유저의 **역할(buyer vs seller)**과 **카드 ID(`1-5`)**를 실시간으로 비교하여, 서로 정확히 교차되는 건에 대해 **`⚡ 교환 가능!`** 뱃지를 달아줍니다.

---

## 🗓️ 업데이트 기록 (2026-06-04)
- **최초 프로젝트 생성**: Vite React 기반의 프로젝트 뼈대 구축.
- **v2 페이지 & 3x3 그리드 개편**: 구매자/판매자 분기 게이트웨이 및 3x3 슬롯 토글 선택기 탑재.
- **v2.1 카테고리 정제**: 중복 파일 제외한 20개 카테고리 정제 완료 및 누락 카테고리 방어 코딩 적용.
- **v2.2 위하이브 180장 CDN 실시간 맵핑**:
  - Ulixee Hero 크롤러로 위하이브 스티커 가이드 글에서 180장의 실물 이미지 CDN 추출 성공.
  - 3x3 그리드 각각의 슬롯 배경에 **드래곤 실물 카드 렌더러** 장착.
  - 누락된 3개 카테고리(유령 마을, 빛 아래, 하늘나라)를 포함한 20개 전 영역에 실물 카드 3x3 비주얼 완전 매핑.
- **v2.3 카드명 노출 및 카테고리 UI 격리**:
  - 3x3 각 상세 카드 슬롯 하단에 실제 한글 드래곤 명칭을 연동 및 출력.
  - 카테고리 뷰 카드를 대표이미지(contain 비율) + 이름 구조로 나누어 겹침 없는 배열 렌더링 완료.
- **v2.4 인게임 테마 벤치마킹 및 레이아웃 확장 [LATEST]**:
  - 동영상 피드백을 기반으로 **다크 그레이 & 네온 연두/황금 별** 감성의 인게임 디자인 테마를 고스란히 이식.
  - 3x3 도감 진입 시 가로 분할을 풀고 **800px 가로폭의 거대 1열 중앙 집중형 레이아웃** 구현.
  - 개별 카드 비율을 게임 카드의 **세로형 비율(aspectRatio: 0.78)**로 튜닝하고 미선택 시 **흑백/어두운 실루엣 연출 및 회색 별 5성**, 선택 시 **컬러 활성화 및 황금 별 5성** 기믹 도입.
  - 카테고리마다 내가 담은 스티커 개수 현황을 보여주는 **연두색 실시간 획득률 배지(`{selectedCount}/9`)** 적용.
  - 뒤로가기("카테고리 목록") 버튼을 타이틀 최좌측에 고정시켜 정렬 안정성 확보.
  - 상단 로고("DRAGON VILLAGE 3")를 클릭하면 역할 선택 홈으로 복귀하는 UX 동작 추가.
