# 🗺️ 드래곤 빌리지 3 카드교환소 마스터 청사진 (MASTER_BLUEPRINT)

본 문서는 프로젝트의 전체 아키텍처, 핵심 파일 역할, 그리고 최신 기능 명세서(v4.4)를 보관하는 통합 기술 명세입니다.

---

## 🏗️ 시스템 아키텍처 (v4.4)

```mermaid
graph TD
    WithHive[위하이브 커뮤니티] -->|1. Ulixee Hero 동적 스크래핑| Scraper[scripts/scraper.js]
    Scraper -->|2. scraped_images.json 저장| DataUpdate[scripts/updateStickersData.js]
    DataUpdate -->|3. 180개 CDN 주소 매핑| StickersData[src/stickersData.js]
    
    User([사용자 최초 진입]) -->|4. 로그인 모달| LoginGate{일회용 게스트 / 닉네임+비밀번호 세트 가입 & 로그인}
    LoginGate -->|회원 로그인 성공| MainDash[메인 대시보드: 800px 가로폭 단일 열]
    LoginGate -->|게스트 로그인 검증| CheckGuest{기존 회원 및 실시간 접속 중복 체크}
    CheckGuest -->|도용/중복 검출| LoginGate
    CheckGuest -->|통과| MainDash[메인 대시보드: 닉네임 (게스트) 적용]
    
    MainDash -->|카테고리 클릭| Grid3x3[3x3 세로형 카드 도감]
    Grid3x3 -->|좌우 화살표 내비게이션| Grid3x3[카테고리 즉시 전환: ◀, ▶]
    Grid3x3 -->|🟢 줄 수 있는 카드 탭| HavesBasket[줄 수 있는 카드 바구니]
    Grid3x3 -->|🔴 받고 싶은 카드 탭| WantsBasket[받고 싶은 카드 바구니]
    
    HavesBasket & WantsBasket -->|5. 교환 등록 신청| DB[(데이터베이스: Supabase / LocalStorage 폴백)]
    
    DB -->|6. 양방향 실시간 매칭 연산| MatchEngine[매칭 엔진]
    MatchEngine -->|결과 피드 출력| Feed[⚡ 100% 완벽 매칭 / 💡 부분 매칭 뱃지 & 요약]
    
    Feed -->|7. 기능 액션| ActionGate{수정 / 삭제 / 끌올 / 신고 / 댓글}
    ActionGate -->|내 글 수정| EditModal[수정 모달창: 미니 도감 바구니 포함]
    ActionGate -->|댓글 열기| CommentSection[댓글 목록 및 실시간 등록: DB 미작성 시 로컬 폴백]
    ActionGate -->|신고 접수| ReportModal[신고 모달: DB 미작성 시 로컬 폴백]
    
    ActionGate -->|관리자 로그인 간장| AdminDashboard[👑 관리자 대시보드]
    AdminDashboard -->|탭 분기 A| AdminReports[🚨 신고 접수 목록]
    AdminDashboard -->|탭 분기 B| AdminLogs[📋 조치 완료 로그: 로컬스토리지 영구 보존]
    
    Feed -->|8. 1:1 채팅 클릭| ChatWidget[1:1 실시간 채팅 위젯: 수발신 1:1 통합]
    ChatWidget -->|9. 실시간 Presence & 소켓 메시지| DB
    ChatWidget -->|10. 실시간 갱신 & 하이라이트| ChatListUI[목록 실시간 리프레시 및 선택된 방 네온 하이라이팅 매칭]
```

---

## 📂 파일 구조 설명

이 프로젝트의 핵심 파일들은 다음과 같이 배치되어 있습니다:

- **`index.html`**
  - 사이트의 뼈대가 되는 파일입니다. 검색엔진 노출을 위한 정보(SEO)와 한국어 설정이 적용되어 있습니다.
- **`src/main.jsx`**
  - React 앱의 진입점으로, `App.jsx`와 `index.css`를 연결해 브라우저에 화면을 띄워주는 역할을 합니다.
- **`src/App.jsx`**
  - **애플리케이션의 핵심**입니다. 닉네임+비밀번호 기반 로그인/회원가입 제어, 복합 선택 바구니(Haves/Wants), 3x3 스티커 도감, 실시간 양방향 매칭 엔진, 그리고 1:1 채팅창 UI 및 액션 로직이 모두 집약되어 있습니다. 상세 도감 화면 상단에 바로 옆 카테고리로 1초 만에 스위칭할 수 있는 꺾쇠 내비게이션(`<`, `>`)이 내장되어 있습니다. 또한 온라인 접속 유저 목록(`onlineUsers`)을 실시간으로 렌더링해 줍니다. v4.4에서는 **관리자용 조치완료 신고 로그 분할 탭**과 **선택된 채팅방에 대한 연두색 네온 액티브 하이라이트** 스타일링, 그리고 실시간 대화방 목록 자동 리프레시 훅이 전격 반영되었습니다.
- **`src/chatService.js`**
  - **1:1 채팅 비즈니스 로직**을 담당합니다. 닉네임을 가나다순으로 정렬한 `room-닉네임A-닉네임B` 형태의 유일 방 ID 식별 정책이 탑재되어 대화방 다중 분산 생성 버그를 방지하고 실시간 알림음 및 팝업을 완벽 연동합니다.
- **`src/supabaseClient.js`**
  - 데이터 저장소와 통신을 담당합니다. 실제 Supabase의 **posts**, **chat_rooms**, **chat_messages**, **users**, **post_comments**, **reports** 테이블과 연동됩니다. 특히 사용자의 Supabase DB 인스턴스에 `reports` 및 `post_comments` 테이블 스키마가 없는 경우에도 에러를 뱉고 멈추지 않고, 자동으로 로컬스토리지 `mockDB` 폴백(Fallback)으로 우회하도록 철저히 고안되었습니다.
- **`src/index.css`**
  - 앱 전체의 스타일 시트입니다. 인게임 감성의 묵직한 다크 그레이와 네온 연두/노랑 테마 스타일링이 전격 설계되어 글래스모피즘 분위기를 물씬 줍니다.
- **`src/stickersData.js`**
  - 위하이브 동적 크래핑을 통해 빌드된 180장의 정규 스티커 초고화질 CDN 주소 목록과 한글 정명 매핑 로직이 들어있습니다.
- **`supabase_schema.sql`**
  - Supabase 모드 운영 시 필요한 신규 테이블 DDL 생성 쿼리 스펙 파일입니다.
- **`scripts/` (데이터 수집 및 크롭 도구)**
  - `scraper.js`: Ulixee Hero 크롤러를 띄워 위하이브 공식 글에서 180장의 실물 이미지 CDN 주소를 수집합니다.
  - `crop_thumbnails.py`: Pillow 이미지 라이브러리를 통해 20개 카테고리의 1번 이미지 배지 영역을 크롭하여 `public/sticker_images/`에 저장합니다.

---

## ⚡ 교환 매칭 & 실시간 1:1 채팅의 원리 (v4.4)

1. **로그인 및 회원 정보 기억 시스템**:
   - `💾 로그인 & 회원가입 (자동 저장)`: 닉네임과 **비밀번호**를 한 세트로 등록하여 로그인합니다.
   - `⚡ 일회용 게스트로 시작하기`: 닉네임만 적어 SessionStorage에 임시 저장합니다. 이때 게스트 계정의 닉네임 상태는 항상 `닉네임 (게스트)` 형태로 통일되어 할당 및 저장됩니다.
2. **양방향 복합 바구니**:
   - 한 사용자가 줄 수 있는 카드(Haves)와 받고 싶은 카드(Wants)를 동시에 바구니에 담아 글 하나로 등록합니다.
3. **매칭 알고리즘**:
   - **완벽 매칭 (100% 매칭)**: 내가 줄 수 있는 카드와 상대방이 원하는 카드가 겹치고, 동시에 내가 원하는 카드와 상대방이 줄 수 있는 카드가 겹치는 경우 (`⚡ 100% 매칭 완료!` 배지).
   - **부분 매칭**: 어느 한쪽 방향으로만 교환이 매칭되는 경우 (`💡 부분 매칭` 배지).
4. **실시간 1:1 채팅 및 목록 동기화**:
   - **수신/발신 통합**: 두 대화 당사자의 닉네임을 가나다순으로 자동 정렬하여 하나의 고유 방 ID(`room-닉네임A-닉네임B`)를 생성해 매핑하므로, 여러 글에서 동일인에게 말을 걸더라도 방이 중구난방 쪼개지지 않고 **단 하나의 대화방**으로 결합되어 메시지를 수발신합니다.
   - **실시간 대화 목록 업데이트**: 내가 메시지를 보내거나 상대방이 실시간으로 메시지를 전송할 때, 글로벌 메시지 리스너(`subscribeAllMyMessages`) 및 로컬 탭 동기화 리스너(`dv3_chat_update`)가 즉각 `loadChatRooms()`를 호출하여 목록을 항상 최신 메시지와 시각 기준으로 갱신합니다.
   - **목록 하이라이팅**: 채팅방 목록 상에서 현재 사용자가 클릭하여 활성화한 대화방(`room.id === chatActiveRoomId`)에 연두색 네온 테두리와 투명 네온 광채 하이라이팅 효과를 부여하여, 사용자 경험의 매칭성을 완벽하게 제공합니다.
5. **실시간 Presence & 소켓 채팅 연동**:
   - Supabase Presence API 및 로컬스토리지 Heartbeat 폴백 시스템을 통하여, 로그인 중인 유저들의 접속 상태를 실시간 동기화합니다.
6. **글 수정 / 댓글 / 신고 및 관리 권한 체계 & DB 폴백**:
   - **교환글 수정**: 내가 올린 글의 보유/필요 스티커 목록과 연락처 정보를 실시간으로 수정하며, 미니 스티커 도감 인터페이스를 모달 안에 내장해 모바일/PC 모두에서 직관적으로 수정이 가능합니다.
   - **댓글 아코디언 및 신고 접수**: 거래글 하단 댓글 및 부적절한 게시글/댓글 신고 기능이 제공됩니다. 만약 Supabase 상에 `post_comments` 혹은 `reports` 테이블이 만들어져 있지 않아 `relation does not exist` 에러가 감지되는 환경일 경우, 브라우저 콘솔에 경고를 남기고 로컬스토리지 모드로 즉시 우회(Fallback) 처리하여 안전하게 동작시킵니다.
   - **관리자 전용 권한 & 조치 로그**: 로그인 닉네임이 `간장`일 때만 관리자 권한이 부여되며, 누적된 신고 목록을 통합 모니터링하여 대상 게시글/댓글을 원클릭으로 강제 삭제 및 조치 종결할 수 있습니다. 또한, 조치(반려/삭제) 완료된 모든 기록은 로컬스토리지 `dv3_admin_resolved_logs`에 영구히 저장되어 관리자 대시보드 내 **조치 완료 로그** 탭을 통해 언제든 모니터링할 수 있습니다.

---

## 🗓️ 업데이트 기록 (2026-06-06)

- **v3.0** ~ **v4.2**: 일회용 로그인 및 비밀번호 기반 로그인, 양방향 매칭 엔진, 실시간 1:1 채팅, 띵동 알림음, 안 읽은 개수 배지, 우하단 토스트 알림 팝업, 내 정보 사이드 드로어 UI 개발, 연락처 선택 입력화, 3x3 도감 페이지 전환 화살표 내비게이션, 온라인/오프라인 접속 유저 실시간 Presence 등.
- **v4.3 댓글/신고 테이블 부재 대비 자동 로컬 폴백 탑재**:
  - Supabase 데이터베이스 상에 `post_comments` 및 `reports` 테이블이 없는 경우에도 에러를 뿜으며 멈추지 않고, 자동으로 로컬스토리지 `mockDB` 폴백으로 우회해 댓글/신고를 가능하게 함.
- **v4.4 관리자 신고 로그 추가 및 1:1 채팅방 목록 실시간 매칭 동기화 [LATEST]**:
  - 관리자가 신고 내역을 반려하거나 대상(글/댓글)을 강제 삭제했을 때 조치 내역이 사라지지 않도록 관리자 대시보드 내에 **조치 완료 로그** 탭을 추가하고 로컬스토리지(`dv3_admin_resolved_logs`)에 실시간 영구 보존함.
  - 채팅방 목록 상에서 현재 입장한 활성 대화방에 인게임 감성의 **연두색 네온 액티브 하이라이팅 효과**를 입혀 visual 매칭 완성.
  - 글로벌 새 메시지 구독 콜백 및 로컬스토리지 갱신 커스텀 이벤트(`dv3_chat_update`)에 대해 `loadChatRooms()`를 즉각 트리거하여 대화 목록의 실시간 갱신 문제 해결.
