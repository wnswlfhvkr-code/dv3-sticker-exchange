# 🗺️ 드래곤 빌리지 3 카드교환소 마스터 청사진 (MASTER_BLUEPRINT)

본 문서는 프로젝트의 전체 아키텍처, 핵심 파일 역할, 그리고 최신 기능 명세서(v4.0)를 보관하는 통합 기술 명세입니다.

---

## 🏗️ 시스템 아키텍처 (v4.0)

```mermaid
graph TD
    WithHive[위하이브 커뮤니티] -->|1. Ulixee Hero 동적 스크래핑| Scraper[scripts/scraper.js]
    Scraper -->|2. scraped_images.json 저장| DataUpdate[scripts/updateStickersData.js]
    DataUpdate -->|3. 180개 CDN 주소 매핑| StickersData[src/stickersData.js]
    
    User([사용자 최초 진입]) -->|4. 로그인 모달| LoginGate{일회용 게스트 / 닉네임+비밀번호 세트 가입 & 로그인}
    LoginGate -->|회원 로그인 성공| MainDash[메인 대시보드: 800px 가로폭 단일 열]
    LoginGate -->|게스트 로그인 검증| CheckGuest{기존 회원 및 실시간 접속 중복 체크}
    CheckGuest -->|도용/중복 검출| LoginGate
    CheckGuest -->|통과| MainDash
    
    MainDash -->|카테고리 클릭| Grid3x3[3x3 세로형 카드 도감]
    Grid3x3 -->|좌우 화살표 내비게이션| Grid3x3[카테고리 즉시 전환: ◀, ▶]
    Grid3x3 -->|🟢 줄 수 있는 카드 탭| HavesBasket[줄 수 있는 카드 바구니]
    Grid3x3 -->|🔴 받고 싶은 카드 탭| WantsBasket[받고 싶은 카드 바구니]
    
    HavesBasket & WantsBasket -->|5. 교환 등록 신청| DB[(데이터베이스: Supabase / LocalStorage)]
    
    DB -->|6. 양방향 실시간 매칭 연산| MatchEngine[매칭 엔진]
    MatchEngine -->|결과 피드 출력| Feed[⚡ 100% 완벽 매칭 / 💡 부분 매칭 뱃지 & 요약]
    
    Feed -->|7. 기능 액션| ActionGate{수정 / 삭제 / 끌올 / 신고 / 댓글}
    ActionGate -->|내 글 수정| EditModal[수정 모달창: 미니 도감 바구니 포함]
    ActionGate -->|댓글 열기| CommentSection[댓글 목록 및 실시간 등록 폼]
    ActionGate -->|신고 접수| ReportModal[신고 모달: 사유 선택 및 누적]
    ActionGate -->|관리자 로그인 간장| AdminDashboard[👑 관리자 대시보드: 신고 검토 및 강제 제재 조치]
    
    Feed -->|8. 1:1 채팅 클릭| ChatWidget[1:1 실시간 채팅 위젯 (우하단 플로팅)]
    ChatWidget -->|9. 실시간 Presence & 소켓 메시지| DB
```

---

## 📂 파일 구조 설명

이 프로젝트의 핵심 파일들은 다음과 같이 배치되어 있습니다:

- **`index.html`**
  - 사이트의 뼈대가 되는 파일입니다. 검색엔진 노출을 위한 정보(SEO)와 한국어 설정이 적용되어 있습니다.
- **`src/main.jsx`**
  - React 앱의 진입점으로, `App.jsx`와 `index.css`를 연결해 브라우저에 화면을 띄워주는 역할을 합니다.
- **`src/App.jsx`**
  - **애플리케이션의 핵심**입니다. 닉네임+비밀번호 기반 로그인/회원가입 제어, 복합 선택 바구니(Haves/Wants), 3x3 스티커 도감, 실시간 양방향 매칭 엔진, 그리고 1:1 채팅창 UI 및 액션 로직이 모두 집약되어 있습니다. 특히 상세 도감 화면 상단에 바로 옆 카테고리로 1초 만에 스위칭할 수 있는 꺾쇠 내비게이션(`<`, `>`)이 내장되어 있습니다. 또한 온라인 접속 유저 목록(`onlineUsers`)을 닉네임 옆과 채팅창 헤더에 인디케이터 배지(🟢 온라인 / ⚪ 오프라인)로 실시간으로 렌더링해 줍니다. 이번 v4.0에서는 게스트 로그인 시 가입 회원 도용을 방지하는 정식 유저 체크 및 접속 중복 닉네임 탐지 로직이 전격 보강되었습니다.
- **`src/chatService.js`**
  - **1:1 채팅 비즈니스 로직**을 담당합니다. 로컬스토리지 모드 및 Supabase 모드 모두 지원하며, `window.dispatchEvent`와 `storage` 이벤트 리스너를 활용해 서로 다른 브라우저 탭/창 간에 실시간 1:1 양방향 통신을 보장합니다.
- **`src/index.css`**
  - 앱 전체의 스타일 시트입니다. 인게임 감성의 묵직한 다크 그레이와 네온 연두/노랑 테마 스타일링이 전격 설계되어 글래스모피즘 분위기를 물씬 줍니다.
- **`src/stickersData.js`**
  - 위하이브 동적 크래핑을 통해 빌드된 **180장의 정규 스티커 초고화질 CDN 주소 목록**과 한글 정명 매핑 로직이 들어있습니다.
- **`src/supabaseClient.js`**
  - 데이터 저장소와 통신을 담당합니다. 데이터베이스 연결이 완료되었을 때 실제 Supabase의 **posts**, **chat_rooms**, **chat_messages**, **users**, **post_comments**, **reports** 테이블과 연동되어 실시간 대시보드를 생성하며, 백업 및 로컬 가동 시에는 LocalStorage 백업 엔진(`mockDB`)으로 자연스럽게 스위칭되어 100% 무중단 동작을 지원합니다.
- **`supabase_schema.sql`**
  - Supabase 모드 운영 시 필요한 신규 테이블 DDL 생성 쿼리 스펙 파일입니다.
- **`scripts/` (데이터 수집 및 크롭 도구)**
  - `scraper.js`: Ulixee Hero 크롤러를 띄워 위하이브 공식 글에서 180장의 실물 이미지 CDN 주소를 수집합니다.
  - `crop_thumbnails.py`: Pillow 이미지 라이브러리를 통해 20개 카테고리의 1번 이미지 배지 영역을 크롭하여 `public/sticker_images/`에 저장합니다.

---

## ⚡ 교환 매칭 & 실시간 1:1 채팅의 원리 (v4.0)

1. **로그인 및 회원 정보 기억 시스템**:
   - `💾 로그인 & 회원가입 (자동 저장)`: 닉네임과 **비밀번호**를 한 세트로 등록하여 로그인합니다. 최초 로그인 시 자동으로 계정이 가입 등록(`users` 테이블)되며, 이후 로그인 시 비밀번호가 다르면 진입을 차단합니다. 로컬 세션 자격 증명(Credentials)을 세트로 보존 및 기억합니다.
   - `⚡ 일회용 게스트로 시작하기`: 닉네임만 적어 SessionStorage에 임시 저장합니다. **이때 해당 닉네임이 기존에 이미 등록된 회원 계정이거나 현재 온라인 유저 리스트(`onlineUsers`)에 존재할 경우 경고 알림과 함께 로그인을 강력 차단합니다.** 임시 세션은 브라우저 탭을 닫으면 자동으로 소멸됩니다.
2. **양방향 복합 바구니**:
   - 한 사용자가 줄 수 있는 카드(Haves)와 받고 싶은 카드(Wants)를 동시에 바구니에 담아 글 하나로 등록합니다.
3. **매칭 알고리즘**:
   - **완벽 매칭 (100% 매칭)**: 내가 줄 수 있는 카드와 상대방이 원하는 카드가 겹치고, 동시에 내가 원하는 카드와 상대방이 줄 수 있는 카드가 겹치는 경우 (`⚡ 100% 매칭 완료!` 배지).
   - **부분 매칭**: 어느 한쪽 방향으로만 교환이 매칭되는 경우 (`💡 부분 매칭` 배지).
4. **실시간 1:1 채팅**:
   - 피드에서 다른 사람의 글에 있는 `1:1 채팅`을 누르면 즉시 1:1 대화방이 생성되고, 우하단 채팅 팝업에서 대화가 시작됩니다.
   - 로컬스토리지 모드에서는 브라우저의 Storage 이벤트를 Listen하여, 탭을 두 개 열어두고 대화할 때 실시간으로 메시지가 전송 및 수신됩니다.
5. **실시간 Presence & 소켓 채팅 연동**:
   - **Supabase Realtime 메시징**: Supabase 모드가 활성화되면 `chat_messages` 테이블의 `postgres_changes` 이벤트를 실시간으로 구독(Subscribe)하여 새로운 메시지가 추가되는 즉시 대화 창에 수신되어 출력됩니다.
   - **실시간 접속 여부 표시**: Supabase Presence API 및 로컬스토리지 Heartbeat 폴백 시스템을 통하여, 로그인 중인 유저들의 접속 상태를 실시간 동기화합니다. 피드 닉네임 옆 및 채팅 헤더에서 실시간 접속 여부(🟢 온라인 / ⚪ 오프라인) 상태를 감지할 수 있습니다.
6. **글 수정 / 댓글 / 신고 및 관리 권한 체계**:
   - **교환글 수정**: 내가 올린 글의 보유/필요 스티커 목록과 연락처 정보를 실시간으로 수정하며, 미니 스티커 도감 인터페이스를 모달 안에 내장해 모바일/PC 모두에서 직관적으로 수정이 가능합니다.
   - **댓글 아코디언**: 거래글 하단에 달린 댓글을 접고 펼치며, 실시간으로 댓글 피드를 확인 및 작성합니다.
   - **신고 시스템**: 유해/허위 게시물이나 불쾌한 댓글을 감지해 사유별로 신고를 접수합니다.
   - **관리자 전용 권한**: 로그인 닉네임이 `간장`일 때만 관리자 권한이 부여되며, 누적된 신고 목록을 통합 모니터링하여 대상 게시글/댓글을 원클릭으로 강제 삭제 및 조치 종결할 수 있습니다.

---

## 🗓️ 업데이트 기록 (2026-06-06)

- **v3.0 양방향 교환 & 1:1 실시간 채팅 및 로그인 개편**:
  - 일회용 로그인 및 복합 양방향 장바구니, 로컬 실시간 채팅 구현.
- **v3.1 닉네임+비밀번호 회원 기억 로그인 기능 탑재**:
  - 비밀번호 기반 로그인 & 자동 회원가입을 로컬스토리지 DB로 이식 완료.
- **v3.2 Supabase 실시간 원격 DB 연동 및 유저 인증 API 연계**:
  - 제공된 Supabase API URL 및 Key를 로컬 서버에 동기화하고 users 인증 스키마 추가.
- **v3.3 카테고리 즉시 전환 화살표 내비게이션 구현**:
  - 3x3 도감 상세 보기 화면에서 상단 타이틀 양옆의 `<`와 `>` 버튼을 통해 이전/다음 도감 페이지로 즉시 전환할 수 있는 내비게이션 설계 완료.
- **v3.4 실시간 접속 여부(Presence) 및 실시간 채팅 구현**:
  - 피드 게시물 카드, 채팅 헤더, 대화방 리스트에 실시간 접속 여부를 알려주는 초록색 인디케이터 탑재 및 웹소켓 연계.
- **v3.5 Supabase 원격 자동 설정 완료**:
  - 에이전트의 PostgreSQL 직접 접속 설정을 활용하여 Supabase의 `chat_messages`, `users` 테이블에 대한 실시간 수신(Publication) 연동 쿼리를 직접 원격 실행하여 설정을 완벽하게 끝마침.
- **v3.6 실시간 알림음(띵동), 안 읽은 메시지 수 배지 및 토스트 미리보기 팝업 구현**:
  - 새 메시지 수신 시 청아한 띵동 알림음 재생.
  - 대화방 리스트 및 우하단 플로팅 버튼에 실시간 누적 안 읽은 메시지 수 네온 배지(● N) 표출.
  - 우하단에 4초간 머무는 슬라이드업(slide-up) 미리보기 팝업(Toast) 구현 및 토스트 클릭 시 해당 대화방으로 순간이동(Auto Focus Redirect) 지원.
  - 외부 배포 서버(Vercel)에 환경변수(`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`) 동기화 및 최종 배포 릴리즈 성공.
- **v3.7 다중 요구사항 개선 및 사이드 "내 정보" 탭(Drawer UI) 구축**:
  - 우측 슬라이딩 및 글래스모피즘 오버레이를 장착한 "내 정보 및 등록 내역" 드로어 패널 개발.
  - 계정 변경 로그인 시 타인의 글이 내 글로 오인식되던 권한 버그 해결.
  - 카테고리 썸네일/도감 이미지에 `title` 속성을 매핑해 브라우저 툴팁 오동작 해결.
  - 3x3 도감 상세 보기 우상단 대표 이미지 클릭 차단.
  - 교환 신청 연락처 입력 필수 해제 및 연락처 미기입 시 연락하기 버튼 숨김 렌더링.
- **v3.8 실시간 대화 정렬/시간 표시 고도화 및 '내 글 끌어올리기(끌올)' 구현**:
  - Supabase snake_case 반환 필드와 camelCase 불일치를 포맷터로 개편하여 실시간 정렬 및 알림 완전 정상화.
  - 마지막 대화 업데이트 시간을 오늘/어제/요일/날짜로 나누어 똑똑하게 구분 표출.
  - 피드 카드 및 내 정보 드로어에 "끌올" 버튼을 이식하여 데이터베이스의 `created_at` 필드를 현재 시각으로 갱신하는 기능 탑재.
- **v3.9 글 수정, 댓글, 신고 및 관리자 전용 탭 개편**:
  - 본인 등록 글 수정 기능(수정 모달창 및 모달 내 컴팩트 스티커 바구니 편집 패널 장착) 및 내 정보 드로어 연동.
  - 피드 거래글 하단 댓글 접기/펼치기 아코디언, 실시간 댓글 목록 및 댓글 작성/삭제 연동 완료.
  - 유해 게시글 및 댓글 사유별 신고 기능 팝업 접수 처리 연계.
  - 로그인 유저가 `간장` 계정일 경우 상단 헤더에 `👑 관리자` 골드 배지 추가 및 누적 신고 내역 처리용 관리자 대시보드 팝업 개발 완료.
- **v4.0 게스트 로그인 보안 강화 및 중복 계정 보호 처리 [LATEST]**:
  - 일회용 게스트로 로그인 시도 시, 기존 데이터베이스에 가입된 회원의 닉네임(예: `간장` 등)을 기입하면 게스트 로그인을 차단하고 정식 회원 로그인을 유도하도록 경고 제어.
  - 게스트 닉네임이 현재 온라인 유저 목록(`onlineUsers`)에 접속 중일 경우에도 닉네임 겹침 및 소유권 오인식을 막기 위해 로그인을 차단 처리 완료.
